# Base URL of the SeeEvent backend API.
# Local dev points at the Express backend in ./backend (default port 4000).
# In production (Netlify) override this via the VITE_API_BASE_URL environment variable.
VITE_API_BASE_URL=http://localhost:4000
