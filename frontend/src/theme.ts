import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    primary: { main: '#214457' },
    secondary: { main: '#F2D555' },
  },
  typography: {
    fontFamily: '"Noto Sans", sans-serif',
  },
});
